Routing 
--> Vue Router 
	RouterLink, RouterView 
	composables - useRouter, useRoute 
	
-> routes { path: '/....', component: CompA } 
	  { path: '/:id', component: CompB } 
	  { path: '/:pathMatch(.*)*', name: 'Notfound', component: CompC} 
	  	to={ name:'Notfound' }
	  { path: '/product-:afterProduct(.*)' 

-- programmitcally change route 
	router.push({....}); 
	router.replace({...}); 
	


Install the Vue Router package 
npm i vue-router@4 


File: src\router\index.js 
import { createRouter, createWebHistory } from "vue-router";

const routes = [

];

const router = createRouter({
    history: createWebHistory(import.meta.env.BASE_URL), 
    routes: routes
})

export default router;

File: main.js 

createApp(App).use(router).mount("#app");



Pinia 
- state - reactive data held by the App - returns an object
- actions - methods which mutates/updates the state, running logic, can be async/sync 
- getters - computed properties which retrieve updated values whenever the state 		changes, takes the state object as input and returns a computed value
	    -> derives the values and cache it until the dependencies change 









